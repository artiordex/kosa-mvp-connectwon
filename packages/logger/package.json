{
  "name": "@connectwon/logger",
  "version": "1.0.0",
  "description": "ConnectWon 로거 모듈 (Winston 기반)",
  "private": true,
  "type": "module",
  "sideEffects": [
    "./dist/**/*.js"
  ],
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/packages/logger/index.d.ts",
      "import": "./dist/packages/logger/index.js"
    },
    "./logger-types": {
      "types": "./dist/packages/logger/logger-types.d.ts",
      "import": "./dist/packages/logger/logger-types.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist",
    "prepublishOnly": "pnpm run clean && pnpm run build",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "logform": "^2.7.0",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0",
    "winston-transport": "^4.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^9.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.11",
    "pnpm": ">=9.0.0"
  }
}
