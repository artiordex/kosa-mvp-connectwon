// =================================================================
// 🎯 MUST HAVE - 필수 외부 라이브러리들
// =================================================================

/**
 * 1. 아이콘 라이브러리
 * 거의 모든 UI에서 아이콘 필요
 */
// Lucide React (가장 추천)
import { Search, User, Settings, ChevronDown } from 'lucide-react';

// 또는 Heroicons
import { MagnifyingGlassIcon, UserIcon } from '@heroicons/react/24/outline';

// 또는 React Icons (가장 많은 아이콘)
import { FiSearch, FiUser, FiSettings } from 'react-icons/fi';
import { AiOutlineSearch } from 'react-icons/ai';

/**
 * 2. 애니메이션 라이브러리
 * 부드러운 UX를 위한 필수 라이브러리
 */
// Framer Motion (가장 강력)
import { motion, AnimatePresence } from 'framer-motion';

const AnimatedCard = () => (
  <motion.div
    initial={{ opacity: 0, y: 20 }}
    animate={{ opacity: 1, y: 0 }}
    exit={{ opacity: 0, y: -20 }}
    transition={{ duration: 0.2 }}
  >
    카드 내용
  </motion.div>
);

// 또는 React Spring (성능 최적화)
import { useSpring, animated } from '@react-spring/web';

/**
 * 3. 폼 라이브러리
 * 복잡한 폼 관리를 위한 필수
 */
// React Hook Form (가장 추천)
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';

// Zod를 이용한 스키마 검증
const schema = z.object({
  email: z.string().email(),
  password: z.string().min(6)
});

/**
 * 4. 날짜/시간 라이브러리
 * 날짜 처리를 위한 필수
 */
// Day.js (가볍고 빠름)
import dayjs from 'dayjs';
import relativeTime from 'dayjs/plugin/relativeTime';
import 'dayjs/locale/ko';

// 또는 Date-fns (모듈러)
import { format, parseISO, addDays } from 'date-fns';
import { ko } from 'date-fns/locale';

// =================================================================
// 🚀 HIGH VALUE - 고가치 라이브러리들
// =================================================================

/**
 * 5. 차트/데이터 시각화
 * 대시보드, 통계 페이지에 필수
 */
// Recharts (React 전용, 간단)
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';

// 또는 Chart.js (더 강력)
import { Chart as ChartJS, CategoryScale, LinearScale, PointElement, LineElement } from 'chart.js';
import { Line as ChartLine } from 'react-chartjs-2';

// 또는 D3.js (최고 수준 커스터마이징)
import * as d3 from 'd3';

/**
 * 6. 드래그 앤 드롭
 * 파일 업로드, 정렬에 유용
 */
// React Dropzone (파일 업로드)
import { useDropzone } from 'react-dropzone';

const FileUpload = () => {
  const { getRootProps, getInputProps } = useDropzone({
    accept: {
      'image/*': ['.jpeg', '.jpg', '.png']
    },
    onDrop: (files) => console.log(files)
  });

  return (
    <div {...getRootProps()}>
      <input {...getInputProps()} />
      파일을 드래그하거나 클릭하세요
    </div>
  );
};

// React Beautiful DnD (리스트 정렬)
import { DragDropContext, Droppable, Draggable } from 'react-beautiful-dnd';

/**
 * 7. 가상화 (Virtual Scrolling)
 * 대용량 데이터 리스트에 필수
 */
// React Window (가볍고 빠름)
import { FixedSizeList as List } from 'react-window';

// 또는 React Virtualized (더 많은 기능)
import { List as VirtualizedList, AutoSizer } from 'react-virtualized';

/**
 * 8. 이미지 처리
 * 이미지 최적화, 크롭 등
 */
// React Image Crop
import ReactCrop from 'react-image-crop';
import 'react-image-crop/dist/ReactCrop.css';

// 또는 React Avatar Editor
import AvatarEditor from 'react-avatar-editor';

// =================================================================
// 🎨 UI/UX ENHANCEMENT - UI/UX 향상 라이브러리들
// =================================================================

/**
 * 9. 색상 선택기
 * 테마 설정, 디자인 도구에 유용
 */
// React Color
import { ChromePicker, SketchPicker, BlockPicker } from 'react-color';

/**
 * 10. 코드 에디터
 * 개발자 도구, 설정 페이지에 유용
 */
// Monaco Editor (VS Code 에디터)
import Editor from '@monaco-editor/react';

// 또는 CodeMirror 6
import { EditorView } from '@codemirror/view';
import { javascript } from '@codemirror/lang-javascript';

/**
 * 11. 마크다운 렌더러
 * 문서, 블로그에 필수
 */
// React Markdown
import ReactMarkdown from 'react-markdown';
import remarkGfm from 'remark-gfm';
import rehypeHighlight from 'rehype-highlight';

/**
 * 12. QR 코드 생성
 * 공유, 결제에 유용
 */
// QRCode.js
import QRCode from 'qrcode.react';

// 또는 React QR Code
import { QRCodeSVG } from 'qrcode2';

// =================================================================
// 🔧 UTILITY - 유틸리티 라이브러리들
// =================================================================

/**
 * 13. 클립보드
 * 복사/붙여넣기 기능
 */
// React Copy to Clipboard
import { CopyToClipboard } from 'react-copy-to-clipboard';

// 또는 modern approach
const copyToClipboard = async (text: string) => {
  await navigator.clipboard.writeText(text);
};

/**
 * 14. Intersection Observer
 * 무한 스크롤, 지연 로딩
 */
// React Intersection Observer
import { useInView } from 'react-intersection-observer';

const LazyComponent = () => {
  const { ref, inView } = useInView({
    threshold: 0,
    triggerOnce: true,
  });

  return (
    <div ref={ref}>
      {inView && <ExpensiveComponent />}
    </div>
  );
};

/**
 * 15. 텍스트 처리
 * 마스킹, 포맷팅
 */
// React Text Mask
import MaskedInput from 'react-text-mask';

// 전화번호, 신용카드 번호 마스킹
const phoneNumberMask = ['(', /[1-9]/, /\d/, /\d/, ')', ' ', /\d/, /\d/, /\d/, '-', /\d/, /\d/, /\d/, /\d/];

// =================================================================
// 📊 ADVANCED FEATURES - 고급 기능 라이브러리들
// =================================================================

/**
 * 16. 지도
 * 위치 서비스, 매장 찾기
 */
// React Leaflet (오픈소스)
import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';

// 또는 Google Maps
import { GoogleMap, LoadScript, Marker } from '@react-google-maps/api';

/**
 * 17. 캘린더
 * 일정 관리, 예약 시스템
 */
// React Big Calendar
import { Calendar, momentLocalizer } from 'react-big-calendar';
import moment from 'moment';

// 또는 React Calendar
import Calendar from 'react-calendar';
import 'react-calendar/dist/Calendar.css';

/**
 * 18. 트리뷰
 * 폴더 구조, 조직도
 */
// React Tree
import Tree from 'rc-tree';

// 또는 React Sortable Tree
import SortableTree from 'react-sortable-tree';

/**
 * 19. 플로우 다이어그램
 * 워크플로우, 프로세스 다이어그램
 */
// React Flow
import ReactFlow, {
  MiniMap,
  Controls,
  Background,
  useNodesState,
  useEdgesState
} from 'reactflow';

/**
 * 20. 오디오/비디오 플레이어
 * 미디어 재생
 */
// React Player
import ReactPlayer from 'react-player';

// 오디오만 필요한 경우
import { Howl } from 'howler';

// =================================================================
// 📦 패키지 추천 우선순위
// =================================================================

/**
 * 🥇 TIER 1 - 즉시 추가 추천
 * 거의 모든 프로젝트에서 필요
 */
const tier1Libraries = [
  'lucide-react',           // 아이콘
  'framer-motion',          // 애니메이션
  'react-hook-form',        // 폼
  'dayjs',                  // 날짜
  'react-dropzone',         // 파일업로드
];

/**
 * 🥈 TIER 2 - 선택적 추가
 * 프로젝트 성격에 따라 필요
 */
const tier2Libraries = [
  'recharts',               // 차트
  'react-window',           // 가상화
  'react-color',            // 색상선택
  'react-markdown',         // 마크다운
  'react-copy-to-clipboard', // 클립보드
];

/**
 * 🥉 TIER 3 - 특수 용도
 * 특정 기능이 필요할 때만
 */
const tier3Libraries = [
  '@monaco-editor/react',   // 코드에디터
  'react-big-calendar',     // 캘린더
  'react-leaflet',          // 지도
  'reactflow',              // 플로우차트
  'react-player',           // 미디어
];

// =================================================================
// 🚀 실제 패키지 추가 예시
// =================================================================

/**
 * package.json에 추가할 의존성들
 */
const recommendedDependencies = {
  // 필수 (Tier 1)
  "lucide-react": "^0.263.1",
  "framer-motion": "^10.16.4",
  "react-hook-form": "^7.45.4",
  "dayjs": "^1.11.9",
  "react-dropzone": "^14.2.3",

  // 선택적 (Tier 2)
  "recharts": "^2.8.0",
  "react-window": "^1.8.8",
  "react-color": "^2.19.3",
  "react-markdown": "^8.0.7",
  "react-copy-to-clipboard": "^5.1.0",

  // 특수 용도 (Tier 3) - 필요시 추가
  "@monaco-editor/react": "^4.5.1",
  "react-big-calendar": "^1.8.2",
  "react-leaflet": "^4.2.1",
  "reactflow": "^11.10.1",
  "react-player": "^2.13.0"
};

// =================================================================
// 💡 통합 사용 예시
// =================================================================

// 여러 라이브러리를 조합한 고급 컴포넌트 예시
const AdvancedFileUpload = () => {
  const { getRootProps, getInputProps } = useDropzone({
    accept: { 'image/*': [] },
    onDrop: (files) => {
      // Framer Motion으로 애니메이션
      // React Hook Form으로 검증
      // Day.js로 타임스탬프 생성
    }
  });

  return (
    <motion.div
      {...getRootProps()}
      whileHover={{ scale: 1.02 }}
      whileTap={{ scale: 0.98 }}
      className="border-dashed border-2 p-8 text-center cursor-pointer"
    >
      <input {...getInputProps()} />
      <Search className="mx-auto mb-4" size={48} />
      <p>파일을 드래그하거나 클릭하세요</p>
    </motion.div>
  );
};
