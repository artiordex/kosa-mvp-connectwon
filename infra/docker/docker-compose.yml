# Description : docker-compose.yml - 📌 도커 컴포즈 설정 파일
# Author      : Shiwoo Min
# Date        : 2025-09-06

version: '3.8'
# 컨테이너 올리기: docker compose up -d
# 빌드 캐시 정리: docker compose build --no-cache
# 초기빌드: docker compose build --no-cache --progress=plain
# 루트에서 빌드: docker compose -f infra/docker/docker-compose.yml build --no-cache
# Docker 빌드 캐시 완전 제거: docker builder prune -af

services:
  # Next.js (웹 프론트엔드)
  web:
    build:
      context: ../../
      dockerfile: ./infra/docker/Dockerfile.web
    container_name: web:latest
    environment:
      - NODE_ENV=production
      - PORT=3000
    expose:
      - "3000"
    # Dockerfile의 CMD 사용 (command 제거)
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 10s
      timeout: 3s
      retries: 10

  # Express.js API (백엔드)
  api:
    build:
      context: ../../
      dockerfile: ./infra/docker/Dockerfile.api
    container_name: api:latest
    environment:
      - NODE_ENV=production
      - PORT=8000
    expose:
      - "8000"
    # Dockerfile의 CMD 사용 (command 제거)
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8000/health"]
      interval: 10s
      timeout: 3s
      retries: 10

  # Nginx 리버스 프록시
  nginx:
    image: nginx:1.27-alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web:
        condition: service_healthy
      api:
        condition: service_healthy

  # PostgreSQL
  postgres:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.db
    image: postgres:17
    container_name: postgres
    env_file: [../../.env]
    ports:
      - "5432:5432"
    command: >
      postgres -c config_file=/etc/postgresql/postgresql.conf
              -c hba_file=/etc/postgresql/pg_hba.conf
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Redis (캐시/세션 스토어)
  redis:
    image: redis:7-alpine
    container_name: redis
    env_file: [../../.env]
    ports:
      - "6379:6379"
    command: >
      sh -c 'exec redis-server
        --appendonly yes
        --appendfsync everysec
        --requirepass "$$REDIS_PASSWORD"
        --maxmemory "${REDIS_MAXMEMORY:-512mb}"
        --maxmemory-policy "${REDIS_EVICTION:-allkeys-lru}"'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $${REDIS_PASSWORD} ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 10

  # RedisInsight (모니터링 UI)
  redisinsight:
    image: redis/redisinsight:latest
    container_name: redisinsight
    ports:
      - "5540:5540"
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - redisinsight:/data

volumes:
  pg_data:
  redis_data:
  redisinsight:
