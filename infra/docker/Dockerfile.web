# Description : Dockerfile.web - 📌 웹 프론트엔드용 Docker 이미지 빌드 파일
# Author : Shiwoo Min
# Date : 2025-09-06
# 09-15 - 버전 고정(corepack prepare pnpm@...) 제거 → package.json의 packageManager(10.7.0) 준수

# Next.js 기반 React 웹 애플리케이션
FROM node:22-alpine3.22 AS build

# 작업 디렉토리 설정
WORKDIR /app

# pnpm 패키지 매니저 활성화
RUN corepack enable
WORKDIR /app

# pnpm 패키지 매니저 활성화
RUN corepack enable

# 의존성 파일들을 먼저 복사
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY tsconfig.json tsconfig.base.json ./

# packages와 apps 디렉토리 전체 복사 (의존성 해결을 위해)
COPY packages ./packages
COPY apps/web/package.json ./apps/web/package.json

# 의존성 설치 (lockfile 기반)
RUN pnpm install --frozen-lockfile --filter=@connectwon/web

# 소스는 마지막에 복사
COPY apps/web ./apps/web

# Next.js 웹 애플리케이션 빌드 (정적 파일 생성)
RUN pnpm --filter @connectwon/web build

# Next.js standalone 모드 실행용 최소 이미지
FROM node:22-alpine3.22 AS production

# 작업 디렉토리 설정
WORKDIR /app

# 프로덕션 환경 변수 설정
ENV NODE_ENV=production

# Next.js standalone 빌드 결과물 복사
COPY --from=build /app/apps/web/.next/standalone ./
COPY --from=build /app/apps/web/.next/static ./.next/static
COPY --from=build /app/apps/web/public ./public

# 웹 서버 포트 노출 (Next.js 기본값: 3000)
EXPOSE 3000

# 컨테이너 시작 시 Next.js 서버 실행
CMD ["node", "server.js"]
