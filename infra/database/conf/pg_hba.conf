# Description : pg_hba.conf - 📌 PostgreSQL 인증 및 접근 제어 설정
# Author      : Shiwoo Min
# Date        : 2025-09-04

# TYPE   DATABASE   USER   ADDRESS         METHOD

# 로컬 유닉스 소켓 (컨테이너 내부)
local   all        all                    scram-sha-256

# IPv4/IPv6 클라이언트 접근
# 개발 단계: 0.0.0.0/0, ::/0 허용 (운영 전환 시 반드시 CIDR 좁히기)
host    all        all    0.0.0.0/0       scram-sha-256
host    all        all    ::/0            scram-sha-256

# 필요 시 특정 관리자만 로컬 루프백 허용 예시
# host   all        postgres 127.0.0.1/32 scram-sha-256
