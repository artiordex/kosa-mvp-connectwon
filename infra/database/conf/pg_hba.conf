/**
 * Description : pg_hba.conf - 📌 PostgreSQL 인증 및 접근 제어 설정
 * Author : Shiwoo Min
 * Date : 2025-09-04
 */

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# 로컬 유닉스 소켓 연결
local   all             all                                     trust

# 내부 Docker 네트워크에서 접근 허용
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5

# 관리자만 접속 허용할 경우:
# host    all           postgres        127.0.0.1/32            trust

# 개발 환경에서는 trust로 단순화 가능 (보안 ↓)
# host    all           all             0.0.0.0/0               trust
